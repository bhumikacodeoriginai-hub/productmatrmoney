# Advaita Mobile

The mobile experience is planned as an Expo Router React Native application sharing tokens and domain types with `packages/shared`. The web prototype in `apps/web` is the presentation surface for this frontend-only milestone; the mobile app scaffold is reserved for the native implementation phase so it can be tested on Android and iOS simulators without pretending a responsive web shell is native.
