import Link from "next/link";
import { ArrowRight, Check, LockKeyhole, ShieldCheck } from "lucide-react";
import { PublicNav } from "./public-nav";
import { Footer } from "./site-footer";
export function PublicPage({ eyebrow,title,description,children }: {eyebrow:string;title:string;description:string;children:React.ReactNode}){return <><PublicNav/><main><section className="section" style={{paddingBottom:55,background:"linear-gradient(180deg,#fff5f4,#fffafa)"}}><div className="container" style={{maxWidth:850}}><span className="eyebrow">{eyebrow}</span><h1 className="display" style={{fontSize:"clamp(42px,6vw,68px)",lineHeight:1,margin:"18px 0"}}>{title}</h1><p className="muted" style={{fontSize:16,lineHeight:1.7,maxWidth:630}}>{description}</p></div></section><section className="section" style={{paddingTop:55}}><div className="container" style={{maxWidth:850}}>{children}</div></section></main><Footer/></>}
export function ContentCard({title,children,icon=<ShieldCheck/>}:{title:string;children:React.ReactNode;icon?:React.ReactNode}){return <div className="side-card" style={{marginBottom:14,padding:25}}><span className="feature-icon" style={{marginBottom:15}}>{icon}</span><h2 style={{fontSize:18,margin:"0 0 9px"}}>{title}</h2><p className="muted" style={{fontSize:13,lineHeight:1.7,margin:0}}>{children}</p></div>}
export function PublicCta(){return <div className="cta-panel" style={{marginTop:35}}><div><span className="eyebrow">Start at your pace</span><h2>Ready to meet differently?</h2><p>No pressure. Just a thoughtful first step.</p></div><Link href="/onboarding" className="btn btn-primary">Create a profile <ArrowRight size={15}/></Link></div>}
