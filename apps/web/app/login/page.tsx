"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowLeft, ArrowRight, Check, Heart, LockKeyhole, ShieldCheck } from "lucide-react";
import { Brand } from "@/components/ui";

export default function Login(){const[mode,setMode]=useState<"mobile"|"email">("mobile");const[step,setStep]=useState<"entry"|"otp">("entry");const[value,setValue]=useState("");const[otp,setOtp]=useState(["","","","","",""]);return <div className="auth-page"><section className="auth-visual"><Brand dark/><div><span className="eyebrow" style={{color:"#d8c9ff"}}>Come as you are</span><h1 className="display">A more thoughtful way to meet.</h1><p>Advaita brings people together with clarity, warmth and the privacy to move at your own pace.</p></div><div className="auth-bullets"><div className="auth-bullet"><ShieldCheck size={16}/> Every profile is created with intention</div><div className="auth-bullet"><LockKeyhole size={16}/> You choose what to share</div><div className="auth-bullet"><Heart size={16}/> Built for meaningful relationships</div></div><small style={{color:"#929bb3",fontSize:10}}>© 2026 Advaita Matrimony</small></section><section className="auth-form-wrap"><div className="auth-form"><Link href="/" className="nav-link" style={{display:"inline-flex",gap:7,alignItems:"center"}}><ArrowLeft size={14}/> Back to home</Link>{step==="entry"?<><h2>Welcome back.</h2><p>Sign in to continue your journey. Your privacy is always in your hands.</p><div style={{display:"flex",gap:7,padding:4,background:"#f1f1f6",borderRadius:11}}>{["mobile","email"].map(item=><button key={item} className={`btn ${mode===item?"btn-primary":""}`} style={{flex:1,minHeight:38,fontSize:11,background:mode===item?undefined:"transparent",color:mode===item?undefined:"var(--ink-soft)"}} onClick={()=>setMode(item as "mobile"|"email")}>{item==="mobile"?"Mobile number":"Email address"}</button>)}</div><div className="form-field"><label htmlFor="login-value">{mode==="mobile"?"Mobile number":"Email address"}</label><input id="login-value" className="form-input" placeholder={mode==="mobile"?"+91 98765 43210":"you@example.com"} value={value} onChange={e=>setValue(e.target.value)} /></div><button className="btn btn-primary auth-submit" onClick={()=>setStep("otp")}>Continue <ArrowRight size={16}/></button><div className="divider">or</div><button className="btn btn-ghost auth-submit" onClick={()=>setStep("otp")}>Continue with demo account</button><div className="auth-foot">New to Advaita? <Link href="/onboarding">Create your profile</Link></div></>:<><button className="nav-link" style={{display:"inline-flex",gap:7,alignItems:"center",border:0,background:"none",padding:0}} onClick={()=>setStep("entry")}><ArrowLeft size={14}/> Change {mode}</button><h2>Enter your code.</h2><p>We sent a six-digit verification code to <strong>{value||"your device"}</strong>. <span className="muted">Demo code: 123456</span></p><div className="otp-row">{otp.map((digit,i)=><input key={i} className="otp-input" inputMode="numeric" maxLength={1} aria-label={`Digit ${i+1}`} value={digit} onChange={e=>{const next=[...otp];next[i]=e.target.value.slice(-1);setOtp(next);}} />)}</div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",margin:"15px 0 24px",fontSize:11}}><span className="muted">Code expires in 00:42</span><button className="table-action">Resend code</button></div><Link href="/app" className="btn btn-primary auth-submit">Verify and continue <Check size={16}/></Link><div className="auth-foot"><LockKeyhole size={13} style={{verticalAlign:"middle"}}/> Your login is secured with one-time verification.</div></>}</div></section></div>}
