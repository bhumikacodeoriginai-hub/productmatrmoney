"use client";
import { useState } from "react";
import { Check, Send } from "lucide-react";
import { PublicPage } from "@/components/public-page";
export default function Contact(){const[sent,setSent]=useState(false);return <PublicPage eyebrow="Contact" title="We’re here to help." description="Tell us what you need. This preview simulates support submission so the future support API has a clear place to connect.">{sent?<div className="side-card" style={{textAlign:"center",padding:55}}><Check size={35} color="var(--teal)"/><h2>Message received.</h2><p className="muted">Our support team will get back to you soon.</p></div>:<div className="side-card" style={{maxWidth:600,padding:28}}><label className="form-field"><span>Your email</span><input className="form-input" placeholder="you@example.com"/></label><label className="form-field"><span>How can we help?</span><select className="form-input"><option>Account or profile</option><option>Safety concern</option><option>Membership</option><option>Something else</option></select></label><label className="form-field"><span>Message</span><textarea className="form-input" style={{minHeight:130,paddingTop:12}} placeholder="Tell us a little more..."/></label><button className="btn btn-primary" onClick={()=>setSent(true)}>Send message <Send size={15}/></button></div>}</PublicPage>}
